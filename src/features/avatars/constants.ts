import { MAX_LEVEL } from '@/features/profile/constants';

/** Given to every new account, and the fallback when a stored id is unknown. */
export const DEFAULT_AVATAR_ID = 'rookie';

export const MIN_REQUIRED_LEVEL = 1;
export const MAX_REQUIRED_LEVEL = MAX_LEVEL;

export const AVATAR_CONFIG_KEY = 'football-home-ui:avatar-levels:v1';
